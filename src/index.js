let cvs = document.querySelector("canvas");
let context = cvs.getContext('2d');
context.fillStyle = "#e9f";
context.fillRect(50,20,80,80);
context.strokeStyle = "#f00";
context.strokeRect(50,20,80,80);